/*
IF.. , IF..ELSE, IF...ELSE IF...ELSE
*/
int main(){

    // IF
    if (condition) {

        // ถ้า condition เป็น จริง ทำอะไรก็ว่าไป

    }


    // IF...ELSE
    if (condition) {

        // ถ้า condition เป็นจริง(true) ทำอะไรก็ว่าไป
    
    }
    else{

        // ถ้า condition เป็นเท็จ(false) ถึงจะเข้ามาทำใน else
    }


    // IF...ELSE IF...ELSE
    if (condition_1) {

        // ถ้า condition_1 เป็นจริง(true) ทำอะไรก็ว่าไป

    }
    else if (condition_2) {

        // ถ้า condition_1 เป็นเท็จ จะเข้าไปเช็ค condition_2 ถ้าจริงก็เข้ามาทำ

    }
    else if (confition_n) {

        // ถ้ามีหลาย condition ก็ใส่มาเรื่อยๆ ได้เลย

    }
    else {

        // ถ้า condition ด้านบนไม่จริงเลย ก็เข้ามาทำ else สุดท้ายนี้แหละ

    }


    return 0;
}
